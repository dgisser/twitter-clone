<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".activities.TimelineActivity">
    <item
        android:id="@+id/miProfile"
        android:orderInCategory="100"
        android:title="@string/profile"
        android:icon="@drawable/ic_profile"
        android:onClick="onProfileView"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/miCreateTweet"
        android:orderInCategory="101"
        android:title="CreateTweet"
        android:icon="@drawable/ic_create_tweet"
        android:onClick="onCreateTweet"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/miActionProgress"
        android:title="@string/loading"
        android:visible="false"
        android:orderInCategory="102"
        app:showAsAction="always"
        app:actionLayout="@layout/action_view_progress" />

</menu>
